[workspace]
members = [
    "crates/cellrune",
    "crates/cellrune-interop",
    "crates/cellrune-mcp",
    "bindings/common",
    "bindings/python",
    "bindings/node",
    "release-tests/integration",
]
default-members = ["crates/cellrune"]
exclude = [
    "fuzz",
    "release-tests/package-consumer",
]
resolver = "3"

[workspace.package]
version = "0.1.1"
edition = "2024"
rust-version = "1.88"
license = "MIT"
readme = "README.md"
repository = "https://github.com/emulette/cellrune"
publish = false

[workspace.dependencies]
# These are compatibility floors, not snapshots. The committed lockfile supplies the reproducible
# graph; the nightly floor and newest-compatible runs exercise both ends of these requirements.
calamine = { version = "0.36.0", default-features = false }
cap-std = ">=3.4.1, <5"
clap = { version = ">=4.0.27, <5", features = ["derive"] }
libm = { version = ">=0.1.3, <0.3", default-features = false }
napi = { version = ">=3.0.0, <4", default-features = false, features = ["napi8"] }
napi-build = ">=2.2.1, <3"
napi-derive = ">=3.2.0, <4"
pyo3 = { version = "0.29.0", features = ["macros"] }
pyo3-build-config = "0.29.0"
quick-xml = { version = "0.41.0", default-features = false }
rmcp = { version = ">=2.0.0, <3", default-features = false, features = [
    "macros",
    "server",
    "transport-io",
] }
schemars = "1.0.0"
serde = { version = "1.0.194", features = ["derive"] }
serde_json = "1.0.127"
sha2 = { version = ">=0.9.0, <0.12", default-features = false }
tokio = { version = ">=1.44.2, <2", features = [
    "io-std",
    "macros",
    "rt-multi-thread",
    "sync",
] }
tokio-util = "0.7.9"
tracing = "0.1.25"
tracing-subscriber = { version = "0.3.20", default-features = false, features = [
    "fmt",
    "std",
] }
zip = { version = ">=7.2.0, <9", default-features = false, features = [
    "deflate-flate2-zlib-rs",
] }

[workspace.lints.rust]
unsafe_code = "forbid"

[workspace.lints.clippy]
dbg_macro = "deny"
todo = "deny"
unimplemented = "deny"
